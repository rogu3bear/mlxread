name = "mlxread-web"
main = "build/_worker.js"
compatibility_date = "2026-04-22"
upload_source_maps = true

[build]
command = "bash ./scripts/build-edge.sh"

[assets]
directory = "./target/site"
binding = "ASSETS"

# No D1 / data layer: MLXRead's site is a static marketing + demo surface.
